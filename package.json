{
  "private": true,
  "workspaces": [
    "services/*",
    "frontend",
    "cicd"
  ],
  "scripts": {
    "build:frontend": "lerna run build --scope frontend",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "test:ci": "lerna run --concurrency 1 test:ci",
    "coverage": "lerna run --concurrency 1 coverage",
    "prettier": "lerna run prettier",
    "prettier:ci": "lerna run prettier:ci",
    "deploy:dev": "lerna run deploy -- --stage dev",
    "deploy:staging": "lerna run deploy -- --stage staging",
    "deploy:prod": "lerna run deploy -- --stage prod",
    "test:e2e:dev": "lerna run --concurrency 1 test:e2e",
    "test:e2e:staging": "lerna run --concurrency 1 test:e2e",
    "test:e2e:prod": "echo 'not running e2e testing in production'",
    "remove:all:dev": "lerna run --concurrency 1 remove --scope 'frontend' -- --stage dev && lerna run --concurrency 1 remove --ignore 'frontend' -- --stage dev",
    "remove:all:staging": "lerna run --concurrency 1 remove --scope 'frontend' -- --stage staging && lerna run --concurrency 1 remove --ignore 'frontend' -- --stage staging",
    "remove:all:prod": "lerna run --concurrency 1 remove --scope 'frontend' -- --stage prod && lerna run --concurrency 1 remove --ignore 'frontend' -- --stage prod",
    "print:name": "lerna run print:name"
  },
  "devDependencies": {
    "jest-junit": "^6.2.1",
    "lerna": "^3.11.1",
    "prettier": "^1.16.4"
  },
  "author": "Erez Rokah",
  "license": "MIT",
  "name": "serverless-monitoring-app",
  "resolutions": {
    "@types/react": "16.8.3"
  }
}
