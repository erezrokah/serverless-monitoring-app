export const onUpdateDataEntry = `subscription onUpdateDataEntry {
    updateDataEntry {
        id
        name
        url
        averageLatencyMs
        lastSample
        status
        logo
    }
  }`;
